# Hydra Project (config)

Two configuration files mark `status: oficial` with different `limiar` values
(config-a.yaml = 0.61, config-b.json = 0.79). It is unknown which one is the current
canonical source. Project history in HISTORICO.md.
