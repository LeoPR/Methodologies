# Projeto Hydra (config)

Dois arquivos de configuracao marcam `status: oficial` com valores de `limiar` diferentes
(config-a.yaml = 0.61, config-b.json = 0.79). Nao se sabe qual e a fonte canonica vigente.
Historico do projeto em HISTORICO.md.
